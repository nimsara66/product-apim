<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<!--
  Phase 7 (incremental) smoke: one gateway-INVOCATION runner using the same block config as testng-v2.xml's
  IntegrationV2-Gateway block, run against the distributed lane (-Dapim.topology=distributed). Exercises the
  full runtime path distributed: publish -> event-hub sync to GW -> invoke through the GW passthrough to the
  shared NodeAppServer backend (the GW is multi-homed onto SHARED_NETWORK to reach "nodebackend").
-->
<suite name="Integration Tests v2 (distributed GW smoke)" parallel="tests" thread-count="1">
    <listeners>
        <listener class-name="org.wso2.am.integration.cucumbertests.utils.listeners.BlockLifecycleListener"/>
        <listener class-name="org.wso2.am.integration.cucumbertests.utils.listeners.BlockScopeListener"/>
    </listeners>
    <test name="IntegrationV2-Gateway" parallel="classes" thread-count="1">
        <parameter name="blockLabel" value="gateway"/>
        <parameter name="initTenantUsers" value="true"/>
        <parameter name="initBackend" value="true"/>
        <classes>
            <class name="org.wso2.am.integration.cucumbertests.runners.block.GatewayRestInvocationRunner"/>
        </classes>
    </test>
</suite>
